"""WikiWaves — automated Wikipedia-to-podcast pipeline."""

from loguru import logger


def main() -> None:
    """CLI entry point (placeholder)."""
    logger.info("WikiWaves pipeline ready.")
    print("WikiWaves pipeline ready. Run with: uv run python -m wikiwaves.fetcher")
